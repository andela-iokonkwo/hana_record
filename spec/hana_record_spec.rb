require 'spec_helper'

describe HanaRecord do
  it 'has a version number' do
    expect(HanaRecord::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
